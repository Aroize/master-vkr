package care.visify.ui.kit.theme.values

internal val baseLightPalette = Colors(
    label = Colors.Label(),
    frame = Colors.Frame(),
    dividers = Colors.Dividers(),
)

internal val baseDarkPalette = Colors(
    label = Colors.Label(),
    frame = Colors.Frame(),
    dividers = Colors.Dividers(),
)

